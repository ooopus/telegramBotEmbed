.git
.gitignore
target/
# OS-specific files
.DS_Store
*.swp
*.swo
# IDE files
.idea/
.vscode/
# Local secrets or configs if any (though config.toml might be versioned if it's a template)
# config.toml # If it contains secrets and is not meant to be in the image by default
*.log
cache_data/ # If cache is generated locally during tests
# Add any other files or directories that are not needed for the build
# e.g. local test data, large assets not used by the final binary.
